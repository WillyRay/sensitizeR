% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitize.R
\name{join_csv_files}
\alias{join_csv_files}
\title{Sensitize Functions}
\usage{
join_csv_files(file1, file2, by)
}
\arguments{
\item{file1}{Path to the first CSV file (model input parameters).}

\item{file2}{Path to the second CSV file (model outputs).}

\item{by}{The name of the shared key column to join on (character).}
}
\value{
A data.frame containing all columns from both files, joined on the shared key.
}
\description{
Functions for the sensitizeR package.
}
\details{
This file contains core functions for the sensitizeR package.
Join Parameter and output CSV Files by a Common Column

Join Model Input and Output CSV Files by a Shared Key

This function takes a CSV (.csv or .txt) file containing model input parameters and a CSV file containing model outputs.
The third argument is the shared key column name present in both files. The function returns a dataframe containing all columns
from the first file and all columns from the second file, joined on the shared key. If duplicate columns exist (other than the key),
they are deduplicated if their data matches; otherwise, the function aborts and notifies the user.

\itemize{
\item Both files must contain the shared key column specified by \code{by}.
\item If duplicate columns (other than the key) have different data, the function aborts and returns NULL.
\item Accepts both .csv and .txt files in standard CSV format.
}
}
\examples{
join_csv_files("data1.csv", "data2.csv", "id")
join_csv_files("inputs.txt", "outputs.txt", "run")
}
\seealso{
\code{?sensitizeR}
}
\author{
WillyRay
}
\keyword{internal}
